{
  "type": "object",
  "properties": {
    "id": {"type": "integer"},
    "titulo": {"type": "string"},
    "autor": {"type": "string"},
    "ano": {"type": "integer"},
    "estado": {
      "type": "string",
      "enum": ["disponivel", "emprestado"]
    },
    "genero": {"type": "string"}
  },
  "required": ["id", "titulo", "autor", "ano", "estado"]
}
